"""Phase-1 MCP server — read-only wrapper around the dashboard API.

See `docs/user/mcp-server.md` for the user-facing contract and
`docs/dev-setup.md` for client wiring (Claude Desktop / Cursor / Claude Code).
"""
